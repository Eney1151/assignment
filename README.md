# Assignment: Linear Models & Optimization

## Overview
In this assignment, you will implement a logistic regression classifier 
using stochastic gradient descent variations.

## Topics
- Feature engineering (polynomial expansion)
- Logistic regression
- SGD, Momentum, RMSprop optimization

## Getting Started
1. Clone this repository
2. Install dependencies: `pip install numpy matplotlib jupyter`
3. Open the notebook: `jupyter notebook week01_pa.ipynb`
4. Complete all TODO sections
5. Run all cells and capture output for submission

## Requirements
- Python 3.6+
- NumPy
- Matplotlib
- Jupyter

## Tasks
1. **Implement `expand(X)`** – Add polynomial features
2. **Implement `probability(X, w)`** – Logistic regression prediction
3. **Implement `compute_loss(X, y, w)`** – Cross-entropy loss
4. **Implement `compute_grad(X, y, w)`** – Gradient computation
5. **Train with SGD variants** – Mini-batch SGD, Momentum, RMSprop

## Submission
Submit your completed notebook with all output cells executed.
